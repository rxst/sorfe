// import { IEndPointOptions, EndPoint } from './end.point.decorator';

describe('end.point.decorator', () => {
    describe('prototype check', () => {
        test('Check property for check class as sorfe end point', () => {
            expect(() => 2 + 2).toBe(4);
        })
    })
})
